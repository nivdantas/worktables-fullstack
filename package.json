{
	"name": "worktables-monorepo",
	"private": true,
	"workspaces": [
		"apps/*",
		"packages/*"
	],
	"scripts": {
		"dev": "bun run --filter '*' dev",
		"build": "bun run --filter '*' build",
		"start": "bun run --filter '*' start"
	},
	"devDependencies": {
		"api": "workspace:*",
		"ts-config": "workspace:*",
		"web": "workspace:*"
	}
}
